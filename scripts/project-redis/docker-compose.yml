version: "3.3"

networks:
  default:
    external:
      name: ${APP_NAME}

services:

  redis:
    image: registry.cn-hangzhou.aliyuncs.com/huangyalong/redis-stack-server:7.4.0-v5
    container_name: ${APP_NAME}-redis
    privileged: true
    ports:
      - "${REDIS_PORT}:6379"
    environment:
      - "TZ=Asia/Shanghai"
    volumes:
      - "$PWD/redis-stack.conf:/redis-stack.conf"
      - "$PWD/data:/data"
    restart: unless-stopped
